# Design Brief - 内网 AI 生图协作平台

## 产品概要

- **产品名称**：[待用户确认]
- **产品类型**：Web（内网部署）
- **目标用户**：10 人左右的设计/创作团队，在公司内网使用
- **核心功能**：生成工作台、AI prompt enhance、多模态生成、历史记录管理、管理员后台
- **当前 MVP 补充**：项目首页缩略图、Generate 页图片同页原图预览、MiniMax 多模态生成、Profile 管理、生成数量选择、取消生成

## 设计方向

### 情绪关键词

- **专业** — 面向创作团队，功能强大但界面简洁，专注于生图任务本身
- **高效** — 对话式交互降低学习成本，快速从想法到出图
- **现代** — 参考当前主流 AI 产品（ChatGPT、Midjourney Web）的视觉语言

### 参考产品

| 参考产品 | 喜欢的方面 | 不喜欢的方面 |
|---------|-----------|-------------|
| **ChatGPT** | 对话式交互简单直观，专注内容本身 | 功能相对单一 |
| **Midjourney Web** | 图像展示效果好，操作按钮清晰 | Discord 风格不够专业 |
| **lovart.ai** | 现代感强，输入体验流畅 | - |
| **Linear** | 信息密度高但不杂乱，动画流畅 | 偏开发者风格 |

### 反面参考

- **传统管理后台**：不要 Ant Design / Element UI 那种千篇一律的蓝白企业风格
- **过度设计**：不要满屏渐变、阴影、动画的花哨效果

## 视觉规范

### 色彩方向

- **主题模式**：深色为主（生图工具，深色更能突出图像），支持浅色切换
- **色彩温度**：中性偏冷（专业感）
- **品牌主色方向**：深灰/黑色为底，蓝紫色或青色作为强调色（AI 科技感）
- **强调色方向**：亮色用于主要操作按钮（生成、发送），红色用于警告/错误
- **品牌资产**：无（需从零开始）

### 信息密度

- **密度方向**：适中偏紧凑
- **参考基准**：接近 ChatGPT 的对话密度，图像展示区域宽松
- **理由**：对话历史需要高效利用空间，但生成的图像需要足够展示区域

### 排版方向

- **字体气质**：现代无衬线，干净利落（Inter / SF Pro 风格）
- **中文字体偏好**：系统默认（苹方/思源黑体）
- **标题风格**：与正文差异适中，不需要太夸张

### 交互风格

- **动画程度**：适中——页面切换、消息出现有平滑过渡，但不花哨
- **过渡效果**：快速渐变和位移为主，不要弹跳效果
- **整体节奏**：轻快但不失稳重

## 核心页面视觉备注

### Generate 生成工作台（主界面）

- **核心交互**：用户输入提示词 → 选择分类/风格/比例/数量/模型 → AI enhance（可选）→ 生成 → 等待或取消 → 查看结果
- **视觉方向**：
  - 单栏暗色生成工作台，不使用左侧侧边栏
  - 顶部左侧显示当前项目名称并支持编辑，中间搜索框，右侧返回主页
  - 中间为生成记录流，最新记录在上，旧记录自然滚动到下方
  - 每条记录展示 prompt、模型、比例、风格、时间和操作按钮
  - 生成图片按稳定比例网格展示，`1x / 2x / 4x` 数量下都不能压扁或裁切
  - 底部输入区为正常停靠式生成框，不使用大面积黑色占位，也不遮挡生成记录
- **特殊要求**：
  - 生成中记录高度应稳定，避免生成开始或完成时整体布局跳动
  - 生成中必须提供“取消生成”按钮；取消后立即停止等待并显示取消状态
  - AI enhance 按钮放在输入区右侧辅助区域，状态要明确
  - AI enhance 后的提示词直接回填输入框，用户可继续编辑；不要自动发送
  - 图片本身必须可点击，在当前页面打开原图预览层
  - 原图预览层使用深色半透明背景，居中展示图片，不跳转页面
  - 关闭方式包括：点击背景、点击关闭按钮、按 Esc
  - Upscale、收藏等未接入完整后端闭环的操作必须弱化或标明占位，不要伪装成完整功能

### 项目首页

- **核心交互**：用户登录后看到自己的项目/对话列表，点击项目进入 Generate 页面
- **视觉方向**：
  - 暗色作品管理台布局，顶部固定工具栏
  - 顶部包含品牌、搜索框、新建项目
  - 内容区标题为 Recent Projects，并提供排序、筛选、网格/列表视图切换
  - 卡片网格布局，第一张固定为「新建项目」卡片
  - 项目卡片主视觉优先展示该项目第一张生成图片缩略图
  - 无图片结果时展示低调占位图标
  - 标题、生成类型、时间信息保持紧凑，不抢图片
- **特殊要求**：
  - 缩略图使用真实 `<img>` 展示，保持 `object-fit: cover`
  - 悬停时显示重命名、删除等管理操作
  - 不要把首页做成营销 landing page，打开后应直接进入工作项目列表
  - 搜索框应支持按项目名、提示词和类型过滤
  - 网格/列表切换只改变项目展示密度，不影响项目操作
  - 未完成的语言切换和用户头像菜单不要显示为可操作入口

### 历史记录/图库页面

- **核心交互**：按日期/标签/工作流筛选，点击查看大图
- **视觉方向**：类似 Midjourney Web 的网格布局，支持瀑布流
- **特殊要求**：悬停时显示操作按钮（下载、重新生成）

### 管理员后台

- **核心交互**：当前 MVP 以 Profile 管理为主；后续扩展用户管理、工作流配置、API 配额统计
- **视觉方向**：类似 Linear 的管理面板，左侧导航 + 右侧内容区
- **特殊要求**：
  - 管理入口以 Vue 前端 `/admin` 为准
  - Profile 列表需要清楚展示启用状态、支持模型、优先级、Base URL 和脱敏 key
  - GPU 监控后续用图表展示（温度、显存占用曲线）

### 登录页面

- **核心交互**：输入用户名 + 密码 → 登录
- **视觉方向**：极简居中布局，背景可展示示例作品轮播
- **特殊要求**：错误提示内联显示，不用弹窗

## 状态设计

- **空状态**：简短文案引导 + 操作按钮（如"还没有作品，开始生成第一张图吧"），不用插画
- **加载状态**：
  - 生图过程：进度条 + 预估剩余时间
  - 内容加载：骨架屏（Skeleton）
  - AI 回复：打字机光标效果（可选）
- **错误状态**：内联错误提示，红色边框或背景，不用弹窗；明确告知用户如何修复

## 补充说明

### 组件设计规范（供 dev-builder 参考）

| 组件 | 规范方向 |
|------|---------|
| **按钮** | 圆角中等（8-12px），主按钮醒目（强调色背景），次按钮描边样式 |
| **输入框** | 圆角与按钮一致，聚焦时有明显边框或阴影 |
| **卡片** | 轻微阴影区分层级，圆角与整体风格一致 |
| **生成记录卡片** | 纵向记录流中每张卡片禁止被 flex 压缩；旧记录自然滚动到下方 |
| **图像网格** | 响应式布局，根据屏幕宽度自动调整列数；图片按比例完整预览，不裁切、不压扁 |
| **图片预览层** | 全屏覆盖层，深色背景，原图等比 contain，顶部紧凑工具条显示文件名和关闭按钮 |
| **项目卡片缩略图** | 固定高度图片区，使用真实图片填充；无图时显示占位，不让文字撑乱布局 |
| **底部生成框** | 停靠在页面底部，不悬浮遮挡内容，不依赖大块空白占位；支持 AI enhance、参数选择、1x/2x/4x、生成/取消 |

### 响应式设计

- **桌面端优先**：主要使用场景是桌面浏览器
- **平板适配**：对话布局和图像网格自动调整
- **移动端**：基础功能可用，但不作为重点

---

## 设计交付说明

1. **设计工具**：可使用 Figma / Excalidraw / 其他设计工具制作高保真原型
2. **交付内容**：主界面、历史记录页、管理员后台的关键页面
3. **给开发的输入**：Design Brief + 设计稿（如有）+ Product Spec

---

## 下一步建议

1. **调用 `/design-maker`**：生成完整的高保真设计稿（可选）
2. **调用 `/dev-planner`**：制定开发计划，开始编码实现
